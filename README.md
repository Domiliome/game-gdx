# Tower Defense Game (LibGDX)

Простой и производительный двумерный Tower Defense для платформ **Android**. Проект разработан на языке Java с использованием игрового фреймворка **LibGDX** и построен на чистой объектно-ориентированной архитектуре (ООП) с разделением логики, данных и отрисовки.

---

## 🏗️ Архитектура и Структура проекта


```text
io.github.simple_game.core
│
├── Main.java                        # Точка входа в игру, управляет переключением экранов
│
├── 📂 model                         # Данные, состояние и поведение игрового мира
│   ├── 📂 entity                    # Игровые сущности и объекты
│   │   ├── Entity.java              # Базовый абстрактный класс для всех объектов в мире
│   │   ├── Enemy.java               # Состояние врага (здоровье, скорость, получение урона)
│   │   ├── Projectile.java          # Логика полета самонаводящегося снаряда до цели
│   │   ├── Tower.java               # Логика башни (поиск ближайшей цели, таймер стрельбы)
│   │   └── TowerType.java           # Перечисление видов башен, их цен и базовых параметров
│   │
│   └── 📂 movement                  # Логика перемещения объектов (Паттерн "Стратегия")
│       ├── MovementBehavior.java    # Общий интерфейс для реализации разных типов движения
│       ├── RoadPath.java            # Маршрут уровня, состоящий из ключевых точек (Waypoints)
│       └── WalkMovement.java        # Попиксельное движение наземных врагов строго по вейпоинтам
│
├── 📂 service                       # Игровая логика, сервисы и управление процессами
│   ├── CameraGestureService.java    # Обработка жестов мобильной камеры (Pan, Pinch-to-Zoom, Инерция)
│   ├── CurrencyManager.java         # Управление экономикой игрока (баланс золота, учет жизней)
│   ├── DragAndDropManager.java      # Менеджер перетаскивания башен на карту с проверкой сетки и золота
│   ├── EnemyFactory.java            # Фабрика генерации мобов с прогрессией сложности раундов
│   ├── EntityManager.java           # Хранение списков сущностей и их безопасная очистка из памяти
│   ├── GameLoop.java                # Главный высокоуровневый цикл игры, координирующий такты менеджеров
│   ├── InteractionService.java      # Обработчик одиночных тапов по сетке мира (улучшение башен за золото)
│   ├── ShopService.java             # Легковесный сервис, хранящий доступный ассортимент башен
│   └── WaveManager.java             # Контроллер фаз волн и порционного спавна юнитов по таймеру
│
└── 📂 presentation                  # Слой графического отображения и пользовательского UI
    │
    ├── 📂 screen                    # Мобильные игровые экраны жизненного цикла LibGDX
    │   └── GameScreen.java          # Центральный экран, связывающий воедино слои рендеринга и ввода
    │
    ├── 📂 ui                        # Изолированные Scene2D виджеты интерфейса
    │   ├── ShopPanel.java           # Нижняя адаптивная панель магазина с иконками башен и DragListener
    │   └── TopStatusBar.java        # Верхняя панель статистики (волна, золото, жизни, кнопка пуска)
    │
    └── 📂 view                      # Визуальное представление и координаторы отрисовки
        ├── GameInterface.java       # Менеджер интерфейса, инициализирующий корневую сцену Scene2D Stage
        ├── GameRenderer.java        # Главный координатор рендеринга, вызывающий послойные модули
        │
        └── 📂 renderer              # Модульные низкоуровневые отрисовщики игрового мира
            ├── DebugGridRenderer.java   # Отрисовка линий координатной сетки и радиусов атаки башен
            ├── EntityRenderer.java      # Геометрический рендеринг фигурок врагов и летящих снарядов
            ├── HealthBarRenderer.java   # Расчет процентов здоровья и рисование HP-баров над мобами
            └── WorldSpriteRenderer.java # Отрисовка пиксельной текстуры карты фона и спрайтов башен
```

---

## 🕹️ Текущие игровые механики

*   **Движение по вейпоинтам:** Враги плавно перемещаются по заранее заданным точкам маршрута независимо от FPS (используется `deltaTime`).
*   **ИИ Башен (Поиск цели):** Башня автоматически сканирует карту и выбирает первого вошедшего в её радиус действия врага. Если враг погибает или уходит из радиуса, башня ищет новую цель.
*   **Самонаводящиеся снаряды:** Выпущенные снаряды летят точно за своей целью, пересчитывая вектор направления каждый кадр.
*   **Привязка к сетке (Grid Snapping):** Башни автоматически центрируются по клеткам при клике мышкой или тапе по экрану.
*   **Система улучшений:** При повторном клике на уже построенную башню запускается механика апгрейда, увеличивающая её параметры (урон, радиус, скорость атаки) по формуле.
*   **Отрисовка UI и здоровья:** Рендерер автоматически рисует динамические полоски здоровья (HP bars) над каждым врагом, линии дорог и радиусы действия башен.

---

## 🚀 Команды для сборки и запуска через консоль

Запускать команды необходимо из корневой папки проекта.

### 🖥️ Запуск на ПК (Desktop)
Компиляция общей логики и запуск игры в оконном режиме на компьютере (через LWJGL3):
```bash
./gradlew lwjgl3:run
```
*(Если ваш пусковой модуль на ПК называется по-старому, используйте `./gradlew desktop:run`)*

### 📱 Запуск на Android
Перед запуском убедитесь, что ваш смартфон подключен к ПК по USB, а в настройках устройства включена «Отладка по USB».

**Полная сборка и запуск на устройстве:**
```bash
./gradlew android:installDebug android:run
```

**Очистить кэш и принудительно пересобрать проект (при ошибках сборки):**
```bash
./gradlew clean assembleDebug
```
**Сброс до последнего коммита**
```
git reset --hard HEAD
```

---

## 🛠️ Используемые технологии

*   **Язык программирования:** Java 17+
*   **Игровой движок:** LibGDX (v1.12.x+)
*   **Система автоматизации сборки:** Gradle
*   **Среда разработки:** Android Studio / IntelliJ IDEA
